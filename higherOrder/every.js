// The every() function checks if all elements in the array satisfy a given condition.
const n =[1,2,3,5,11]
const allpostive = n.every(num => num>0) // if all elements are positive then only
console.log(allpostive) // all are positive then true and or else flase